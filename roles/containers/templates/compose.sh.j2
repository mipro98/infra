#!/bin/bash

if [[ -z $1 ]]; then
    echo "No command for docker-compose provided"
    exit 1
fi

{% for service in docker_services %}
docker-compose -f "{{ docker_compose_dir }}/{{ service }}/docker-compose.yml" "$@"
{% endfor %}
